@model IEnumerable<FishBusiness.Models.IMerchantReciept>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Merchant)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalOfReciept)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Merchant.MerchantName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalOfReciept)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IMerchantRecieptID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IMerchantRecieptID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IMerchantRecieptID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
